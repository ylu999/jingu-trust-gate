{
  "id": "fix-add",
  "goal": "Fix the add function in src/add.js so it returns a + b (currently returns a - b). Do not modify multiply.",
  "allowedFiles": ["src/**"],
  "maxRetries": 5,
  "verify": {
    "type": "command",
    "command": "node --test tests/add.test.js",
    "pass": { "exitCode": 0 }
  }
}
